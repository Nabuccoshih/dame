#include <SFML/Graphics.hpp>
#include <iostream>

const int SCR_W = sf::VideoMode::getDesktopMode().width;
const int SCR_H = sf::VideoMode::getDesktopMode().height;

const int APP_H = 600;
const int APP_W = 600;